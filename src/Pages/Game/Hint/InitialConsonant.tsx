import React from 'react';

interface Props {
  id: number;
}
function InitialConsonant({ id }: Props): React.ReactElement {
  return <div>InitialConsonant hint</div>;
}

export default InitialConsonant;
